# name: Poll Link Checker

# on:
#   push:
#     branches: [ main, master ]
#   pull_request:
#     branches: [ main, master ]
#   issue_comment:
#     types: [created, edited]
#   issues:
#     types: [opened, edited]

# jobs:
#   check-poll-links:
#     runs-on: ubuntu-latest
#     name: Check for poll links in repository
    
#     steps:
#     - name: Checkout repository
#       uses: actions/checkout@v4
      
#     - name: Run poll link checker
#       run: |
#         chmod +x .github/scripts/check-poll-links.sh
#         .github/scripts/check-poll-links.sh
        
#     - name: Comment on PR if violations found
#       if: failure() && github.event_name == 'pull_request'
#       uses: actions/github-script@v7
#       with:
#         script: |
#           github.rest.issues.createComment({
#             issue_number: context.issue.number,
#             owner: context.repo.owner,
#             repo: context.repo.repo,
#             body: '❌ **Poll Link Policy Violation**\n\nThis PR contains content that violates our poll link policy. Please review and remove any poll-related links or references.\n\nSee [Discussion Policy](.github/DISCUSSION_POLICY.md) for details.'
#           })